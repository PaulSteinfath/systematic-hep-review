# Analysis parameters
params <- list(
  entropy_unique_threshold = 10,
  entropy_num_bins = 10,
  hedges_sig_level = 0.05,
  hedges_power_level = 0.8,
  r_peak_offset = 0,
  t_peak_offset = 300
)

t_peak_offset <- 300
r_peak_offset <- 0